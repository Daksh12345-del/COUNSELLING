// ===== SARVPRATHAM BACKEND CONFIG =====
// Point this at wherever the backend (the sarvpratham-backend folder) is running.
//
// If the backend is hosted on the SAME domain behind a reverse proxy at /api,
// you can leave this as '/api'.
//
// If the backend runs on a different domain or port, use the full URL, e.g.:
//   window.API_BASE_URL = 'https://api.sarvprathameduconsultants.com/api';
//   window.API_BASE_URL = 'https://sarvprathameduconsultants.com:4000/api';

window.API_BASE_URL = '/api';
